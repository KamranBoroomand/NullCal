# Fonts

Drop your `.woff2` files into this folder and update `src/styles/fonts.css` to match the filenames and weights.

Suggested filenames:

- `NullCalSans-Regular.woff2`
- `NullCalSans-SemiBold.woff2`
- `NullCalMono-Regular.woff2`
